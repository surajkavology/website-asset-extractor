# Backend API base URL
# Development: leave empty (Vite proxy handles /api → localhost:5000)
# Production: set to your Railway backend URL on Vercel
VITE_API_URL=https://your-backend.up.railway.app/api
